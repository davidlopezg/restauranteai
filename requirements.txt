# Dependencias para RestaurantEAI (MVP-0 local + MVP-0.5 HF Space)
# Python 3.11 recomendado (3.13 quitó `audioop` y rompe Gradio 4.x)

# --- Core (MVP-0: agente CLI) ---
httpx>=0.27.0          # Cliente HTTP moderno (async-friendly)
python-dotenv>=1.0.0   # Carga de .env

# --- UI (MVP-0.5: HF Space) ---
gradio>=4.44.0,<5.0    # Framework UI para Space público (pineado <5 por compatibilidad con deps)
huggingface_hub>=0.19.3,<1.0  # PIN: HfFolder fue eliminado en v1.0 (Gradio 4.x todavía lo importa)
