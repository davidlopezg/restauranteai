# Dependencias para RestaurantEAI (MVP-0 local + MVP-0.5 HF Space)
# Python 3.11 (3.13 quitó audioop; Gradio 6 funciona bien en 3.11)

# --- Core (MVP-0: agente CLI) ---
httpx>=0.27.0          # Cliente HTTP moderno (async-friendly)
python-dotenv>=1.0.0   # Carga de .env

# --- UI (MVP-0.5: HF Space) ---
gradio>=6.19,<7.0      # Última estable 6.x (incluye bugfixes del bug de UI ChatInterface messages y json_schema_to_python_type)
# huggingface_hub: SIN PIN. Gradio 6.19 ya no usa HfFolder — requiere huggingface-hub>=1.2.0,<2.0 (Gestionado por Gradio)
huggingface_hub>=1.2,<2.0  # Pin compatible con Gradio 6 (lo resuelve automáticamente, pero lo explicitamos para claridad)
