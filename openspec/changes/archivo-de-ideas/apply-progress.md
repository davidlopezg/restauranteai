# Apply Progress — PR 1 + PR 2: Core Modules + Commands Layer

> **Change**: `archivo-de-ideas`
> **Phase**: `sdd-apply` (PR 1 + PR 2)
> **Status**: ✅ PR 1 COMPLETED, ✅ PR 2 COMPLETED
> **Date**: 2026-07-02
> **Chain strategy**: stacked-to-main

---

## Completed Tasks (PR 1)

### Task 1.1 — agents/memoria/ package

- [x] Created `agents/memoria/` directory
- [x] Created `agents/memoria/__init__.py` — exports only `storage` and `formatters` (no `commands` imports — those are PR 2)

### Task 1.2 — agents/memoria/storage.py

- [x] `init_db(db_path=None) -> sqlite3.Connection` — creates dir, WAL mode, row_factory=sqlite3.Row, table + indexes, companion `ideas.md`
- [x] `save_idea(conn, idea, categoria, contexto, origen, origen_skill) -> int`
- [x] `load_ideas(conn, filtro=None) -> list[dict]` — supports categoria, origen_skill, search (LIKE), limit filters
- [x] `get_idea(conn, idea_id) -> Optional[dict]` — returns None for negative/zero IDs
- [x] `edit_idea(conn, idea_id, nuevo_texto, nueva_categoria) -> bool` — updates updated_at
- [x] `delete_idea(conn, idea_id) -> bool`
- [x] `delete_all_ideas(conn) -> int`
- [x] `count_ideas(conn) -> int`
- [x] `export_ideas(conn, export_path) -> Path` — JSON with ensure_ascii=False, indent=2
- [x] `check_duplicate(conn, texto, umbral=0.8) -> list[dict]` — exact match (COLLATE NOCASE) then fuzzy via difflib.SequenceMatcher
- [x] Schema D2: `id, created_at, updated_at, idea, categoria, contexto, confirmada_por_usuario, origen, origen_skill`
- [x] Indexes: `idx_ideas_created_at`, `idx_ideas_categoria`, `idx_ideas_origen_skill`
- [x] PRAGMAs: `journal_mode=WAL`, `foreign_keys=ON`
- [x] Companion `.agent_knowledge/ideas.md` auto-generated by init_db

### Task 1.3 — agents/memoria/formatters.py

- [x] `format_counter(count) -> str` — 0→"", 1→"📁 1 guardada", ≥2→"📁 N guardadas"
- [x] `format_idea_list(ideas) -> str` — per-idea format with 200-char truncation
- [x] `format_save_confirmation(idea, count, contador_activo) -> str` — confirmation + optional counter
- [x] `format_error(msg) -> str` — "⚠️ {msg}"
- [x] `format_duplicate_warning(dup) -> str` — "⚠️ Ya tenés algo parecido (#ID)... ¿Usar /guardar igual?"

All functions are pure (no IO, no DB).

### Task 1.4 — agents/ideas_categorias.json

- [x] Created with 9 categories: concepto, plato, técnica, producto, proveedor, menú completo, ocasión/evento, restricción, otro (escribir)
- [x] Valid JSON, loads and parses correctly

### Task 1.5 — Tests

- [x] `tests/test_memoria_storage.py` — 40 tests covering:
  - init_db (creates dir/table, idempotent, ideas.md companion, md idempotent)
  - save_idea (basic, empty raises, all fields, default origen)
  - load_ideas (empty, all desc, categoria filter, search, search no match, limit, origen_skill filter)
  - get_idea (found, not found, negative id, zero id)
  - edit_idea (text, text+category, not found, empty raises)
  - delete_idea (found, not found)
  - delete_all (deletes all, empty)
  - count (count, empty)
  - export (default path, custom path, empty, structure fields)
  - check_duplicate (exact, case insensitive, fuzzy above threshold, fuzzy below, empty text, empty db, custom umbral)
- [x] `tests/test_memoria_formatters.py` — 17 tests covering all 5 formatter functions

## Completed Tasks (PR 2)

### Task 1.6 — Concurrency tests

- [x] `tests/test_memoria_concurrency.py` — 2 tests:
  - `test_concurrent_writes`: 4 threads with per-thread connections, all 4 distinct IDs, count=4
  - `test_concurrent_read_and_write`: concurrent reader and writer, no exception, consistent results

### Task 2.1 — agents/memoria/commands.py

- [x] `handle_command(mensaje, ultimo_assistant_mensaje, skill_activa, conn, contador_activo) -> Optional[dict]`
- [x] Pending confirmation state for "olvidar todo", "olvidar N", and "guardar igual" after duplicate
- [x] Thread-safe state via `threading.Lock`
- [x] `ARCHIVO_IDEAS_ENABLED` env var guard
- [x] Command regex parsing for all 11 command variants
- [x] Numbered list extraction for `/guardar N` (supports `)` and `.` formats)
- [x] `get_contador_state()` and `toggle_contador_state()`
- [x] `_reset_state()` for testing
- [x] All command handlers delegate to `storage.py` and `formatters.py`

### Task 2.2 — tests/test_memoria_commands.py

- [x] 25 tests covering all command variants:
  - `/guardar [texto]` (3: normal, empty, with skill)
  - `/guardar` sin args (3: with history, without, empty history)
  - `/guardar N` numbered (5: valid, out of range, no list, no history, mixed format)
  - `/guardar igual` (2: after duplicate, without pending)
  - `/editar` (3: existing, inexistent, no text)
  - `/ideas` (4: with ideas, empty, with filter, no match)
  - `/olvidar todo` (1: pedido)
  - `/olvidar N` (2: pedido, inexistente)
  - `/export-ideas` (1)
  - `/ayuda` (1)
  - `/silenciar-contador` (1)
  - Unknown command (1)
  - Non-command returns None (1)

### Task 2.3 — tests/test_memoria_duplicates.py

- [x] 5 tests covering:
  - `test_duplicado_exacto`: exact match → warning with ID + `/guardar igual` prompt
  - `test_duplicado_fuzzy_sobre_umbral`: 85% similar → warning with ID
  - `test_sin_duplicado_fuzzy_bajo_umbral`: 50% similar → saves directly
  - `test_duplicado_db_vacia`: any text on empty DB → saves directly
  - `test_duplicado_con_guardar_igual`: duplicate then force save

### Task 2.4 — tests/test_memoria_counter.py

- [x] 5 tests covering:
  - `test_counter_initial_state`: returns True
  - `test_counter_visible_after_save`: 📁 appears in response
  - `test_counter_silenced`: /silenciar-contador → 📁 hidden
  - `test_counter_reactivated`: toggle twice → 📁 reappears
  - `test_toggle_returns_new_state`: toggle returns new state

### Task 2.5 — tests/test_memoria_rgpd.py

- [x] 7 tests covering:
  - `test_olvidar_todo_sin_confirmacion`: /olvidar todo then non-confirming message → no deletion
  - `test_olvidar_todo_con_confirmacion`: /olvidar todo then olvidar todo → all deleted
  - `test_olvidar_todo_sin_estado_pendiente`: olvidar todo without prior command → error
  - `test_olvidar_todo_multiple_confirmations_rejected`: second confirmation → error
  - `test_olvidar_n_con_confirmacion`: /olvidar 1 then olvidar 1 → deleted
  - `test_olvidar_n_mismatched_confirm`: wrong ID → no deletion
  - Test export with/without ideas

### __init__.py Update

- [x] Added `handle_command`, `get_contador_state`, `toggle_contador_state` imports
- [x] Added commands symbols to `__all__`

## Files Changed

| File | Action | Lines |
|---|---|---|
| `agents/memoria/__init__.py` | CREATED (PR 1) → MODIFIED (PR 2) | ~30 |
| `agents/memoria/storage.py` | CREATED (PR 1) | ~445 |
| `agents/memoria/formatters.py` | CREATED (PR 1) | ~115 |
| `agents/memoria/commands.py` | CREATED (PR 2) | ~430 |
| `agents/ideas_categorias.json` | CREATED (PR 1) | ~11 |
| `tests/test_memoria_concurrency.py` | CREATED (PR 2) | ~80 |
| `tests/test_memoria_commands.py` | CREATED (PR 2) | ~385 |
| `tests/test_memoria_duplicates.py` | CREATED (PR 2) | ~85 |
| `tests/test_memoria_counter.py` | CREATED (PR 2) | ~75 |
| `tests/test_memoria_rgpd.py` | CREATED (PR 2) | ~145 |
| `tests/test_memoria_storage.py` | CREATED (PR 1) | ~450 |
| `tests/test_memoria_formatters.py` | CREATED (PR 1) | ~150 |
| `openspec/changes/archivo-de-ideas/tasks.md` | MODIFIED | Status checkboxes updated |

## Test Commands Run

### PR 2 tests
```bash
python -m pytest tests/test_memoria_concurrency.py tests/test_memoria_commands.py tests/test_memoria_duplicates.py tests/test_memoria_counter.py tests/test_memoria_rgpd.py -v
```

**Result**: 48 passed in 1.83s

### PR 1 regression
```bash
python -m pytest tests/test_memoria_storage.py tests/test_memoria_formatters.py -v
```

**Result**: 57 passed in 2.44s (no regression)

## Deviations from Design

None. All implementation follows the spec and design.

- The categories file was placed at `agents/ideas_categorias.json` (per design §7.4), not `agents/memoria/categorias.json`.
- The `__init__.py` exports only storage and formatters (no commands), as required for PR 1.

## Completed Tasks (PR 3)

### Task 3.1 — app.py:responder() transversal dispatcher

- [x] Added dispatcher block BEFORE the skill switch in `responder()`
- [x] Dispatcher extracts `ultimo_assistant` from historial (empty string if no assistant message)
- [x] Uses local imports (`from agents.memoria.commands import handle_command`) to avoid circular deps
- [x] Opens and closes DB connection per request with `try/finally`
- [x] If `cmd_result is not None`, returns it immediately (skill handler NOT executed)
- [x] If `cmd_result is None`, falls through to existing skill handlers (untouched)
- [x] Exception safety net: catches any error, logs it, returns user-friendly error message
- [x] Empty message guard runs BEFORE the dispatcher (unchanged)

### Task 3.2 — agents/creativo/agent.py CLI dispatcher

- [x] Added `ultimo_assistant = None` state variable to `_loop_ficha`
- [x] Added dispatcher block in `_loop_ficha` BEFORE the `generar_ficha()` call
- [x] After successful ficha response, `ultimo_assistant = ficha`
- [x] Added `ultimo_assistant = None` and dispatcher block to `_loop_ideas_creativas`
- [x] After ideas_creativas response, `ultimo_assistant = respuesta`
- [x] Added `ultimo_assistant = None` and dispatcher block to `_loop_proceso_creativo`
- [x] After proceso_creativo response, `ultimo_assistant = respuesta`
- [x] Added dispatcher block to `main()` ideas mode CLI entry point
- [x] Updated `PROCESO_COMANDOS` with memoria commands:
  - `/guardar`, `/guardar `, `/ideas`, `/ideas `, `/editar`,
  - `/olvidar`, `/olvidar `, `/export-ideas`, `/silenciar-contador`, `/ayuda`

### Task 3.3 — tests/test_regresion_skills.py

- [x] 15 regression tests covering:
  - Ficha skill: normal message (with and without history), command interception, multiple inputs, unknown command
  - Ideas creativas skill: normal message, command interception, multiple inputs
  - Dispatcher edge cases: empty history, exception safety, empty message, no-assistant history, user-only history, multiple commands, olvidar todo confirmation
- [x] All tests mock `call_minimax` (via `agents.creativo.agent.call_minimax` AND `app.call_minimax`) and `init_db` (temp SQLite)
- [x] No real LLM API calls or real `.agent_knowledge/` DB touched

## Files Changed (PR 3)

| File | Action | Lines |
|---|---|---|
| `app.py` | MODIFIED: Added dispatcher block in `responder()` | ~30 added |
| `agents/creativo/agent.py` | MODIFIED: Dispatcher in 3 CLI loops + PROCESO_COMANDOS + main() ideas | ~80 added |
| `tests/test_regresion_skills.py` | CREATED: 15 regression tests | ~280 |
| `openspec/changes/archivo-de-ideas/tasks.md` | MODIFIED: PR 3 tasks marked COMPLETED | 3 checkboxes |

## Test Commands Run

### PR 3 regression tests
```bash
python -m pytest tests/test_regresion_skills.py -v
```

**Result**: 15 passed in 0.73s

### Full test suite (no regression against PR 1 + PR 2)
```bash
python -m pytest tests/test_memoria_storage.py tests/test_memoria_commands.py tests/test_memoria_duplicates.py tests/test_memoria_counter.py tests/test_memoria_rgpd.py tests/test_memoria_concurrency.py tests/test_memoria_formatters.py -v
```

**Result**: 105 passed in 2.77s (all existing tests still pass)

### Grand total
```
120 passed across all memoria test files
```

## Deviations from Design

None. All implementation follows the spec and design.

- The dispatcher in `app.py` uses local imports (inside `responder()`) to avoid circular deps, per design §7.1
- The `PROCESO_COMANDOS` set includes both `/guardar` and `/guardar ` (with trailing space) to prevent false matching, per design §7.3
- The confirmation pattern for `/olvidar` uses the exact string without `/` (design N5), handled by `commands.py`

## Remaining Tasks

None. PR 3 completes the implementation.

## Workload / PR Boundary

- PR 3: ~390 changed lines (app.py + agent.py + test_regresion_skills.py + tasks.md updates)
- All 3 PRs now complete: Core (PR 1) → Commands (PR 2) → Integration (PR 3)
- Ready for verify phase

## Risks (final status)

| Risk | Status |
|---|---|
| R3 — Concurrent writes | ✅ Mitigated: WAL mode + per-thread connections + concurrency tests pass. |
| R4 — Schema rigidity | ✅ Mitigated: categories in editable JSON, no FK constraint on categoria. |
| R5 — Forgotten commands | ✅ Resolved: `/ayuda` lists all commands. |
| R6 — MVP-0.5 regression | ✅ **Resolved**: 15 regression tests pass. All 105 pre-PR-3 tests still pass. Source changes are purely additive. |

## Persisted Task Checkbox Update

Tasks artifact `openspec/changes/archivo-de-ideas/tasks.md` has been updated with ✅ [x] markers for PR 3 tasks (3.1–3.3). All implementation tasks are now complete.
